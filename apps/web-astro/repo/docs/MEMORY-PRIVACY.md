# Memory and privacy

Biography is not inferred into the handbook automatically. Context must be explicitly supplied and approved for synthesis. Raw birth details, credentials, private report prose, and chat history must not enter telemetry. Chart-companion memory maps the authenticated application user to a stable resource and conversation thread. The initial API deliberately does not persist message text or inferred user attributes until durable Mastra memory and deletion controls are qualified.
