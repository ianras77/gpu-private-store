# Intelligence tools

The deterministic fact graph is the first tool boundary. Future Mastra tools must use typed Zod inputs and outputs, enforce ownership internally, carry risk and timeout metadata, and have no arbitrary shell, filesystem, SQL, URL-fetch, deployment, or secret access. Timing tools must return validated timing facts, never prose-generated dates.
