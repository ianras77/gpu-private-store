# Backup and rollback

Before production migration, back up Postgres, application data, and Qdrant metadata. Rollback uses the prior immutable image and additive migration compatibility; it does not rewrite Git history or delete volumes. A report-artifact release is not qualified until old `Reading` rows and legacy renderers remain readable.
