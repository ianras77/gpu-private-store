# Report artifact

`@astro/astro-intelligence` defines the additive `AstrologyReportArtifact` contract. It is sectioned, versioned, provenance-bearing, and retains deterministic fact references while the existing `ReadingOutput` contract remains the compatibility renderer during migration.
