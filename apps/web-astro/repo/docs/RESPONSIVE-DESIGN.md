# Responsive design

The shared experience uses fluid `clamp()` type, a 320px minimum layout, 44px interaction targets, mobile-first stacked hero composition, container-aware proof grids, safe reduced motion, and print overrides. Long-form report work remains editorial: prose uses the existing report artifact, while future atlas chapters should target a 65–75 character measure rather than card-width text.
