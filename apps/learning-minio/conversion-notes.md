# Learning MinIO Conversion Notes

- Split out of the old `learning` mega-package.
- Keeps the original MinIO image and console/API split, but places the console on the main app port and the S3 API on the adjacent support port.

