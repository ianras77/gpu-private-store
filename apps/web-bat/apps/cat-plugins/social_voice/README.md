# Cheshire Cat plugin contract: social_voice

Status:
- implemented through social generation flows in `apps/api/src/services/editorial_service.py`
- documented here as a role/plugin contract, not a standalone plugin package

Intended responsibility:
- generate X-ready short/long/thread variants from approved editorial objects

Role mapping:
- `queen`
