# Cheshire Cat Plugin Contracts

These folders document the logical plugin/tool contracts used by the BAT role pipeline.

Implementation note:
- the active runtime behavior lives in `apps/api/src/services`
- these docs exist so the role/plugin map stays legible even when the repo is not shipping standalone Cheshire Cat plugin packages

Current role chain:
- `researcher`
- `analyst`
- `writer`
- `queen`
