# Cheshire Cat plugin contract: search_connector

Status:
- implemented in `apps/api/src/services/search_connector.py`
- documented here as a role/plugin contract, not a standalone plugin package

Intended responsibility:
- expose SearXNG query helper tools for the Cat runtime
- enforce source grounding metadata on search results

Role mapping:
- `researcher`
