# Learning W&B Conversion Notes

- Split out of the old `learning` mega-package.
- Keeps the original `wandb/local` deployment shape and persisted `/vol/env` state.
- License and API key are node-local runtime settings and should stay in `user-config`.

