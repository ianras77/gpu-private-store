# Qualification

Required gates are server lint, tests, build; web lint, tests, build; Docker Compose config; `/healthz`; House intelligence health; House chat and stream; search; spotlight; feedback; and representative grounded directory/archive queries.

A green liveness probe alone is not release proof. Live RassyMind capability and any provider outage are recorded separately from local deterministic tests. Never report an unproven live lane as passed.
