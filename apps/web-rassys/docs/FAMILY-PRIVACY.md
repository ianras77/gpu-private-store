# Family privacy

Family is private by registry policy. Family/admin context is required before
family agent generation; public channel requests cannot select that agent.
No face, identity, emotion or location inference is implemented here.
