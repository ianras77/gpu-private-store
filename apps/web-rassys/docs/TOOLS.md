# Tools

Tool metadata and risk classes are defined in `packages/mr-rassy-core`. The
current registry exposes compact read/propose/write metadata while the typed
domain adapters remain the source of truth. Privileged write tools are not
available through the public channel endpoint.
