# Operations

This is a Runtipi app. Preserve the three-service Compose topology, project identity, and
persistent `app-data/web-rassyonline` mounts. Validate lint, tests, build, Compose config,
then managed Runtipi lifecycle. Health is not proof of chat; verify authenticated API and
real RassyMind requests separately.
