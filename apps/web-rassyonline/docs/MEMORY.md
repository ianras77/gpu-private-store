# Memory

Existing Rassy Online threads and messages remain canonical during migration. Mastra memory
adoption requires an additive PostgreSQL schema migration and parity qualification first;
resource IDs must equal authenticated user IDs and thread IDs must remain user-scoped.
