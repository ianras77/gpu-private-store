# RassyMind integration

Mastra uses an OpenAI-compatible provider rooted at `RASSYMIND_BASE_URL` with server-only
`RASSYMIND_API_KEY`. Agents use semantic aliases such as `rassy-mind`, `rassy-code`,
`rassy-utility`, and `rassy-fast`; physical model names are never referenced.
